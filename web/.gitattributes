*.sh text eol=lf
*.service text eol=lf
*.yml text eol=lf
*.conf text eol=lf

# dpkg 钩子没有扩展名，必须显式声明 LF。
# 若以 CRLF 检出，目标机上会报 /usr/bin/env: 'bash\r': No such file or directory，
# 错误信息不指向行尾问题，排查成本高。
postinst text eol=lf
prerm text eol=lf
preinst text eol=lf
postrm text eol=lf
